---
title: MakeCode
order: 0
---

# MakeCode Integration

This guide focuses on integration of Jacdac with the
[micro:bit V2](/devices/microbit-educational-foundation/microbitv2/) via [MakeCode for micro:bit](https://makecode.microbit.org/),
You can also program with Jacdac simulators in MakeCode (no hardware required). Jacdac is also
available in [MakeCode Arcade](https://arcade.makecode.com/) and [MakeCode Maker (beta)](https://maker.makecode.com/)

Jacdac does not support the micro:bit V1 or Bluetooth.

## The micro:bit jukebox

The **[micro:bit jukebox](/clients/makecode/microbit-jukebox/)** is a MakeCode program
for the micro:bit V2 that automatically interacts with Jacdac modules.
Before getting into coding Jacdac programs, you can try out the jukebox to
discover the capabilities of Jacdac hardware modules.

## Getting started

-   Open [MakeCode for micro:bit](https://makecode.microbit.org/)
-   Open or create a new project.
-   Add the **Jacdac** extension.
-   You should see a **Modules** drawer in the toolbox.

<video
	aria-label="Adding the Jacdac extension in MakeCode for micro:bit"
	style={{ width: "100%" }}
	playsInline
	controls
	preload="metadata"
	muted
	loop
	poster={`${import.meta.env.BASE_URL}videos/addjacdac.jpg`}
>
	<source src={`${import.meta.env.BASE_URL}videos/addjacdac.webm`} type="video/webm" />
	<source src={`${import.meta.env.BASE_URL}videos/addjacdac.mp4`} type="video/mp4" />
	Sorry, your browser does not support embedded videos.
</video>

### First connection with a micro:bit V2

If you have a program that does not use Jacdac on the micro:bit V2, you will want to start
by downloading an empty program **using Jacdac** onto the micro:bit V2.

-   Click on the menu of the **Download** button, do not connect to the micro:bit V2.
-   Drag and drop the generated .hex file onto the `MICROBIT` drive as usual.

### Connecting to a micro:bit V2

In the editor, you can connect your micro:bit V2 and the editor will automatically communicate with the physical Jacdac modules.

-   Click on the menu of the **Download** button and select **Connect Device** and follow the instructions.
-   Download your program at least once, even if it's empty, so that you get the latest Jacdac runtime in the micro:bit.

<video
	aria-label="Connect to a physical micro:bit"
	style={{ width: "100%" }}
	playsInline
	controls
	preload="metadata"
	muted
	loop
	poster={`${import.meta.env.BASE_URL}videos/connectmicrobit.jpg`}
>
	<source src={`${import.meta.env.BASE_URL}videos/connectmicrobit.webm`} type="video/webm" />
	<source src={`${import.meta.env.BASE_URL}videos/connectmicrobit.mp4`} type="video/mp4" />
	Sorry, your browser does not support embedded videos.
</video>

### Adding blocks for services

When a Jacdac device (physical or simulated) is detected, MakeCode can automatically determine which **extensions** are needed to program with it.

-   After connecting, plug **one or more** modules to the micro:bit. You should see them appear in the **Devices** section.
-   Click on **Add Blocks** to add the extensions for these modules' services in your project.
-   Open the **Modules** drawer in you will find the blocks for your service.

** Pro tip: repeat this procedure if you use other modules! **

<video
	aria-label="Add Jacdac button extension in MakeCode"
	style={{ width: "100%" }}
	playsInline
	controls
	preload="metadata"
	muted
	loop
	poster={`${import.meta.env.BASE_URL}videos/addmodule.jpg`}
>
	<source src={`${import.meta.env.BASE_URL}videos/addmodule.webm`} type="video/webm" />
	<source src={`${import.meta.env.BASE_URL}videos/addmodule.mp4`} type="video/mp4" />
	Sorry, your browser does not support embedded videos.
</video>

## Working without hardware

You can start simulators for any Jacdac service. This is useful if you do not have the hardware module
or you want to simulate specific conditions... like freezing temperatures! For example, to **start a button simulator**,

-   Click on the plus (+) icon in the Jacdac dashboard and select `button`.
-   You should see a button appear in the simulator section of the Jacdac dashboard.
-   Click on **Add Blocks** and follow the same procedure as for physical modules.

<video
	aria-label="Add Jacdac button extension from simulator"
	style={{ width: "100%" }}
	playsInline
	controls
	preload="metadata"
	muted
	loop
	poster={`${import.meta.env.BASE_URL}videos/addbutton.jpg`}
>
	<source src={`${import.meta.env.BASE_URL}videos/addbutton.webm`} type="video/webm" />
	<source src={`${import.meta.env.BASE_URL}videos/addbutton.mp4`} type="video/mp4" />
	Sorry, your browser does not support embedded videos.
</video>

Great you've got your first Jacdac program running on the micro:bit!

## What's next?

-   [User guide](/clients/makecode/user-guide/)
-   [Projects](/clients/makecode/projects/)
-   [Extensions](/clients/makecode/extensions/)

## Where can I get a device?

Browse the [device catalog](/devices) or check out some of the modules below.


See the device catalog pages linked in this section for current specifications.

## Got questions?

See [Jacdac at MakeCode Forum](https://forum.makecode.com/c/jacdac/) for questions/answers about Jacdac and to ask your own questions.

## Found a bug?

If you think you've found a bug, please [file an issue](https://github.com/jacdac/jacdac/issues).

## Advanced

-   The [pxt-jacdac repository](https://github.com/jacdac/pxt-jacdac) contains MakeCode support for many Jacdac services. Each Jacdac service is represented by a separate MakeCode [extension](./makecode/extensions) within the `pxt-jacdac` repository.
-   [Editing in Visual Studio Code](/clients/makecode/code/)
-   [Servers](/clients/makecode/servers/)
